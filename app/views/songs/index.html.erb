<h2>Songs of Tunr</h2>

<% @songs.each do |song| %>
<div class="song">
  <h3><%= link_to song.name, song %></h3>
</div>
<% end %>

<% if user_signed_in? && current_user.admin? %>
<%= link_to "Add Song", new_song_path %>
<% end %>